<!--
Thanks for the pull request. Keeping the boxes below honest is the fastest
way through review — CI checks the same things.
-->

## What this changes

<!-- One or two sentences. Link the issue it closes, if there is one. -->

## Why

<!-- The problem behind the change. Skip if it is obvious from the above. -->

## Checklist

- [ ] `dart format .` leaves the tree unchanged
- [ ] `flutter analyze` reports **No issues found!**
- [ ] `flutter test` passes
- [ ] `cd example && flutter analyze && flutter test` passes
- [ ] New public members carry doc comments
- [ ] The component's page under `doc/` is updated, if its API changed
- [ ] `CHANGELOG.md` has an entry under the unreleased heading

## Public API

- [ ] This PR does not change `lib/seed_ui.dart`

<!--
If it does, list every name added or removed. Removing an exported name is a
breaking change and needs to be called out in the CHANGELOG.
-->
